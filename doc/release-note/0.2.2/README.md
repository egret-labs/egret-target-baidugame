## Baidu MiniGame Support 0.2.2 Release Notes

* Fix an issue where tint shows an error

## 百度小游戏支持库 0.2.2 更新日志
* 重新整理适配层结构
* 模板项目中增加资源缓存功能
* 支持引擎的 `tint` 方法，给当前对象设置填充色
* 支持引擎的 `zIndex` 方法，设置对象的 Z 轴顺序

【2019.7.25】